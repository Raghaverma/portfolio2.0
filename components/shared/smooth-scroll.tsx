"use client";

import type React from "react";

export function SmoothScroll({ children }: { children: React.ReactNode }) {
    // Smooth scroll removed for accessibility - using default browser scrolling
    return <>{children}</>;
}
